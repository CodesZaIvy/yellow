package org.color;

public class Wrapper {
    public static void main(String[] args) {
// Wrapper Class -> provide a way to use primitive data types as objects.
    /*
    Each primitive type has a corresponding wrapper class:
        byte -> Byte
        short -> Short
        int -> Integer
        long -> Long
        float -> Float
        double -> Double
        char -> Character
        boolean -> Boolean
    Autoboxing/Unboxing: Java automatically converts between the primitive types and their corresponding wrapper class objects.
    This is called autoboxing (primitive to wrapper) and unboxing (wrapper to primitive)
     */
 Boolean a = true;
 Character b = 'd';
 Integer c = 209;
 Double d = 3.14;
 String r = "Hers";

 if (c==2090){
     System.out.println("This is a true");

 }else{
     System.out.println("Try again");
 }













    }
}

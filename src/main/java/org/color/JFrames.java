package org.color;
import javax.swing.*;
import java.awt.*;

public class JFrames {
    public static void main(String[] args) {
        // JFrame - a GUI window to add components to

        /*
        // Creates a Frame
        JFrame jFrame = new JFrame();
        // Set size of Frame
        jFrame.setSize(420,420);
        //sets title of Frame
        jFrame.setTitle("Title Here");
        // exit out of application
        jFrame.getDefaultCloseOperation();
        // Resize the window
        jFrame.setResizable(true) ;
        // make the frame visible
        jFrame.setVisible(true);
           // Create an Image Icon
        ImageIcon image = new ImageIcon("/home/pc/Desktop/Bega-.png");
            // change icon of frame
        jFrame.setIconImage(image.getImage());
        //change color of frame
        jFrame.getContentPane().setBackground(new Color(130,170,100));


*/
        MyFrame myFrame = new MyFrame();

    }
}
